          
# ConsistenSee - Плагин для Figma

**ConsistenSee** — это плагин для Figma, представляющий собой линтер дизайн-системы. Плагин помогает дизайнерам и командам поддерживать консистентность в использовании компонентов и цветов в проектах Figma.

## 🎯 Основные функции

### 1. Анализ компонентов
- **Поиск экземпляров компонентов** в выбранных фреймах или секциях
- **Проверка актуальности компонентов** — определение устаревших версий компонентов из библиотек
- **Классификация иконок** — автоматическое определение иконок на основе размеров (до 48x48px) и имени
- **Анализ вложенности** — обнаружение компонентов, вложенных в другие экземпляры
- **Работа с Component Sets** — поддержка наборов компонентов и их вариантов

### 2. Анализ цветов
- **Сканирование цветовых заливок** (fills) и обводок (strokes)
- **Работа с переменными цветов** — определение привязанных переменных и их коллекций
- **Фильтрация служебных цветов** — исключение технических цветов (например, #000000 в группах "source")
- **Конвертация цветов** — преобразование RGB в HEX формат

### 3. Навигация и выделение
- **Прокрутка к элементам** — быстрый переход к найденным компонентам или цветам
- **Групповое выделение** — выбор нескольких элементов одновременно
- **Переключение между страницами** — автоматический переход на нужную страницу при навигации

## 🏗️ Архитектура плагина

### Основные файлы:
- **<mcfile name="manifest.json" path="/Users/Kuzin_K/Dev/manifest.json"></mcfile>** — конфигурация плагина
- **<mcfile name="code.js" path="/Users/Kuzin_K/Dev/code.js"></mcfile>** — основная логика плагина (backend)
- **<mcfile name="ui.html" path="/Users/Kuzin_K/Dev/ui.html"></mcfile>** — пользовательский интерфейс

### Ключевые компоненты:

#### 1. Система кэширования
```javascript
const componentUpdateCache = new Map();
```
Оптимизирует повторные проверки компонентов, используя уникальные ключи для кэширования результатов.

#### 2. Обработчик сообщений
<mcsymbol name="figma.ui.onmessage" filename="code.js" path="/Users/Kuzin_K/Dev/code.js" startline="369" type="function"></mcsymbol> — центральный диспетчер, обрабатывающий команды от UI:
- `check-all` — комплексная проверка компонентов и цветов
- `scroll-to-node` — навигация к элементу
- `select-nodes` — групповое выделение
- `get-component-data` — чтение данных компонентов

#### 3. Функции анализа компонентов
- <mcsymbol name="checkComponentUpdate" filename="code.js" path="/Users/Kuzin_K/Dev/code.js" startline="58" type="function"></mcsymbol> — проверка актуальности компонентов
- <mcsymbol name="processNodeComponent" filename="code.js" path="/Users/Kuzin_K/Dev/code.js" startline="1106" type="function"></mcsymbol> — обработка отдельного компонента
- <mcsymbol name="getComponentCacheKey" filename="code.js" path="/Users/Kuzin_K/Dev/code.js" startline="32" type="function"></mcsymbol> — генерация ключей кэширования

#### 4. Функции анализа цветов
- <mcsymbol name="processNodeColors" filename="code.js" path="/Users/Kuzin_K/Dev/code.js" startline="900" type="function"></mcsymbol> — извлечение цветовой информации
- <mcsymbol name="processVariableBindings" filename="code.js" path="/Users/Kuzin_K/Dev/code.js" startline="1407" type="function"></mcsymbol> — обработка привязок переменных
- <mcsymbol name="rgbToHex" filename="code.js" path="/Users/Kuzin_K/Dev/code.js" startline="880" type="function"></mcsymbol> — конвертация цветов

## 🔄 Логика работы

### Процесс анализа (команда `check-all`):

1. **Инициализация**
   - Проверка выделенных элементов
   - Сбор всех узлов для обработки (включая вложенные)
   - Инициализация структур данных для результатов

2. **Обработка узлов**
   ```javascript
   for (let i = 0; i < nodesToProcess.length; i++) {
     await processNodeSafely(nodesToProcess[i], i);
     // Обновление прогресса каждые 5 элементов
     if (i % 5 === 0) {
       await updateProgress('processing', i + 1, nodesToProcess.length, 'Обработка элементов');
     }
   }
   ```

3. **Анализ компонентов**
   - Для каждого `INSTANCE` получается главный компонент
   - Проверяется актуальность через импорт из библиотеки
   - Определяется тип (компонент/иконка) на основе размеров
   - Извлекается информация о версии из описания

4. **Анализ цветов**
   - Сканирование заливок (`fills`) и обводок (`strokes`)
   - Обработка только видимых сплошных цветов (`SOLID`)
   - Извлечение привязанных переменных и их коллекций
   - Применение фильтров для исключения служебных цветов

5. **Формирование результатов**
   - Сортировка компонентов по имени
   - Группировка по типам (компоненты/иконки)
   - Построение дерева компонентов для навигации
   - Отправка данных в UI

### Система фильтрации:

```javascript
// Исключение технических цветов
if ((nodeData.fill === '#000000' || nodeData.stroke === '#000000') && 
    parent.name && parent.name.toLowerCase() == 'source' && 
    node.parent.type == 'GROUP') {
  return null;
}
```

### Обработка ошибок:
- Глобальные обработчики для WebAssembly ошибок
- Graceful degradation при недоступности API
- Логирование критических ошибок
- Автоматический перезапуск при критических сбоях

## 🎨 Пользовательский интерфейс

### Структура UI:
- **Табы** для переключения между разными типами анализа
- **Поиск** с подсветкой совпадений
- **Группировка результатов** с возможностью сворачивания
- **Прогресс-бар** для длительных операций
- **Отладочная информация** в виде древовидной структуры

### Основные элементы:
- `.tabs` — навигация по разделам
- `.tab-content` — содержимое вкладок
- `#results`, `#icons`, `#colors` — списки найденных элементов
- `.search-container` — поиск по результатам
- `.footer` — нижняя панель с дополнительными функциями

## ⚙️ Настройки и конфигурация

### Permissions в manifest.json:
- `teamlibrary` — доступ к библиотекам команды
- `enablePrivatePluginApi` — использование приватных API
- `documentAccess: "dynamic-page"` — доступ к страницам документа
- `networkAccess` — доступ к внешним API (api.vladey.net)

### Технические ограничения:
- Максимум 200 строк за один вызов `view_files`
- Кэширование результатов для оптимизации
- Обработка узлов с задержками для предотвращения блокировки UI
- Фильтрация скрытых элементов и вложенных экземпляров

## 🚀 Использование

1. **Выберите** фрейм, компонент или группу элементов в Figma
2. **Запустите** плагин Design Polizei
3. **Нажмите** кнопку анализа или используйте соответствующую вкладку
4. **Просмотрите** результаты в удобном интерфейсе
5. **Кликните** на элемент для навигации к нему в документе
6. **Используйте** поиск для быстрого нахождения нужных элементов

Плагин особенно полезен для:
- Аудита дизайн-системы
- Поиска устаревших компонентов
- Анализа использования цветов
- Поддержания консистентности в больших проектах
- Миграции на новые версии компонентов

## 📚 Описание функций

### Основные функции обработки компонентов

#### `getComponentCacheKey(mainComponent)`
- Генерирует уникальный ключ для кэширования компонента
- Использует ID компонента и его версию для создания ключа
- Помогает оптимизировать повторные проверки компонентов

#### `checkComponentUpdate(mainComponent)`
- Проверяет актуальность компонента
- Сравнивает текущую версию с версией в библиотеке
- Возвращает информацию о статусе обновления компонента

#### `processNodeComponent(node, parentSet)`
- Обрабатывает отдельный компонент или экземпляр
- Извлекает метаданные компонента (имя, версия, описание)
- Определяет является ли компонент иконкой
- Собирает информацию о вложенности и родительских компонентах

#### `processComponentSetNode(node, parentSet)`
- Обрабатывает наборы компонентов (Component Sets)
- Извлекает метаданные набора
- Подготавливает данные для отображения в интерфейсе

#### `checkComponentUpdates(componentsResult)`
- Проверяет обновления для списка компонентов
- Обрабатывает каждый компонент в результатах
- Обновляет информацию о статусе компонентов в UI

### Функции обработки цветов и переменных

#### `processNodeColors(node)`
- Анализирует цвета заливки и обводки узла
- Обрабатывает привязки к переменным цвета
- Конвертирует цвета из RGB в HEX формат

#### `processVariableBindings(node, nodeData, propertyType, prefix)`
- Обрабатывает привязки переменных для узла
- Извлекает информацию о переменных и их коллекциях
- Добавляет данные о переменных в результаты

#### `rgbToHex(r, g, b)`
- Конвертирует RGB значения в HEX формат
- Используется для стандартизации представления цветов

### Вспомогательные функции

#### `getNodePath(node)`
- Строит путь к узлу в дереве компонентов
- Используется для навигации и отображения структуры

#### `getDescription(node)`
- Извлекает описание и версию из метаданных узла
- Обрабатывает форматированное описание компонента

#### `isNodeOrParentHidden(node)`
- Проверяет видимость узла и его родителей
- Используется для фильтрации скрытых элементов

#### `isNestedInstance(node)`
- Определяет является ли компонент вложенным
- Помогает в обработке сложных структур компонентов

#### `delay(ms)`
- Создает задержку в выполнении операций
- Используется для оптимизации производительности

#### `updateProgress(stage, current, total, message)`
- Обновляет индикатор прогресса в интерфейсе
- Отображает статус выполнения операций

### Обработчики событий

#### `figma.ui.onmessage`
- Центральный обработчик сообщений от UI
- Управляет всеми основными операциями плагина:
  - Проверка компонентов и цветов
  - Навигация к элементам
  - Групповое выделение
  - Работа с данными компонентов
