# ConsistenSee - Figma Plugin

**ConsistenSee** — это плагин для Figma, представляющий собой линтер дизайн-системы. Плагин помогает дизайнерам и командам поддерживать консистентность в использовании компонентов и цветов в проектах Figma, выявляя устаревшие компоненты, некорректное использование цветов и предоставляя удобные инструменты для навигации и анализа.

## 🎯 Основные функции

### 1. Анализ компонентов
- **Поиск экземпляров компонентов** в выбранных фреймах или секциях
- **Проверка актуальности компонентов** — определение устаревших версий компонентов из библиотек путем сравнения с актуальной версией из библиотеки.
- **Классификация иконок** — автоматическое определение иконок на основе размеров (до 48x48px) и имени (поиск паттернов типа `ЧИСЛО / Имя` или `ЧИСЛО Текст / Имя`).
- **Анализ вложенности** — обнаружение компонентов, вложенных в другие экземпляры (отмечаются как `skipUpdate`).
- **Работа с Component Sets** — поддержка наборов компонентов и их вариантов при анализе.
- **Чтение/Запись метаданных** — возможность сохранять и читать произвольные ключ-значение пары (`customKey`, `customVersion`) в `pluginData` компонентов и наборов компонентов.

### 2. Анализ цветов
- **Сканирование цветовых заливок** (fills) и обводок (strokes) для всех видимых SOLID цветов.
- **Работа с переменными цветов** — определение привязанных переменных, их имен и коллекций.
- **Фильтрация служебных цветов** — исключение технических цветов (например, #000000 или #ffffff в группах с именем "source" или "group", а также фиолетовой обводки Component Set).
- **Конвертация цветов** — преобразование RGB в HEX формат для удобного отображения.

### 3. Навигация и выделение
- **Прокрутка к элементам** — быстрый переход к найденным компонентам или цветам в документе Figma.
- **Групповое выделение** — выбор нескольких элементов одной группы одновременно.
- **Переключение между страницами** — автоматический переход на нужную страницу, если выбранный элемент находится не на текущей странице.

### 4. Пользовательский интерфейс
- **Интерактивный UI** — отображение результатов анализа в удобных вкладках с группировкой и поиском.
- **Прогресс-бар** — визуализация процесса анализа.
- **Отладочная информация** — просмотр необработанных данных анализа в древовидной структуре с возможностью поиска.
- **Splash Screen** — приветственный экран при запуске.
- **Resize Handle** — возможность изменять размер окна плагина.

## 🏗️ Архитектура плагина

Плагин состоит из двух основных частей, взаимодействующих через систему обмена сообщениями Figma API:

1.  **Backend (`code.js`)**: Основная логика плагина, работающая в изолированном окружении Figma. Отвечает за:
    *   Сканирование документа и сбор данных об узлах.
    *   Взаимодействие с Figma API (чтение узлов, импорт компонентов, работа с переменными, навигация).
    *   Обработку данных (определение устаревших компонентов, извлечение цветов, переменных).
    *   Отправку результатов и статуса выполнения в UI.
    *   Обработку команд от UI (например, "прокрутить к узлу", "выбрать узлы").
2.  **Frontend (`ui.html`)**: Пользовательский интерфейс плагина, работающий в iframe. Отвечает за:
    *   Отображение данных, полученных от backend.
    *   Обработку действий пользователя (клики по кнопкам, вкладкам, поиск).
    *   Отправку команд backend через сообщения.
    *   Визуализацию прогресса и ошибок.

### Основные файлы:
-   `manifest.json` — Конфигурация плагина, определяет название, иконку, необходимые разрешения (`teamlibrary`, `enablePrivatePluginApi`, `documentAccess`, `networkAccess`) и точку входа (`code.js`).
-   `code.js` — Основной скрипт backend логики.
-   `ui.html` — HTML, CSS и JavaScript для пользовательского интерфейса.

### Ключевые компоненты backend (`code.js`):

-   `componentUpdateCache`: `Map` для кэширования результатов проверки актуальности компонентов по их ключу.
-   `lastColorsData`: Глобальная переменная для временного хранения данных о цветах между этапами анализа.
-   `figma.ui.onmessage`: Центральный обработчик сообщений от UI.
-   `checkComponentUpdate`: Асинхронная функция для проверки актуальности компонента из библиотеки.
-   `processNodeComponent`: Асинхронная функция для анализа данных компонента/инстанса.
-   `processNodeColors`: Асинхронная функция для анализа цветовых заливок и обводок.
-   `processVariableBindings`: Асинхронная функция для извлечения информации о привязанных переменных.
-   `updateProgress`: Функция для отправки статуса выполнения в UI.
-   Глобальные обработчики ошибок (`window.addEventListener('unhandledrejection', ...)` и `window.addEventListener('error', ...)`): Перехватывают критические ошибки Figma API (особенно связанные с WebAssembly/памятью) и инициируют перезапуск плагина.

### Ключевые компоненты frontend (`ui.html`):

-   HTML структура с вкладками (`.tabs`, `.tab-content`), списками результатов (`#resultsList`, `#iconResultsList`, `#colorResultsList`, `#colorStrokeResultsList`), поиском (`#debugSearchInput`), загрузчиком (`.loader-container`) и областью вывода метаданных (`#componentDataOutput`).
-   CSS стили, использующие Figma Design Tokens (через CSS переменные `:root`) для соответствия интерфейсу Figma.
-   JavaScript обработчики событий для кнопок (`#startCheck`, `#checkAll`, `#readComponentData`, `#setComponentData`, `#clearComponentData`), вкладок (`.tab`), переключателей групп (`.toggle-groups`), поисковых полей.
-   `window.onmessage`: Основной обработчик сообщений от backend, обновляющий UI.
-   `displayGroups`: Функция для рендеринга сгруппированных списков компонентов или цветов.
-   `showPopover`: Функция для отображения детальной информации об элементе при наведении на иконку.
-   `createDebugTree`: Рекурсивная функция для построения интерактивного дерева отладочных данных.
-   `displayComponentData`, `displayResult`: Функции для отображения результатов операций с метаданными компонентов.
-   Логика изменения размера окна плагина (`#resizeHandle`).

## 🔄 Логика работы

Основной сценарий работы плагина запускается по команде `check-all` от UI.

1.  **Инициализация (`figma.ui.onmessage` -> `check-all`)**:
    *   Плагин получает команду `check-all` от UI.
    *   Определяется текущее выделение пользователя (`figma.currentPage.selection`).
    *   Собираются все уникальные узлы для обработки. Это включает выбранные узлы, а также всех их потомков. Учитываются только типы `INSTANCE`, `COMPONENT`, `COMPONENT_SET` и узлы, имеющие заливки или обводки (`hasFillOrStroke`).
    *   Инициализируются пустые структуры данных для сбора результатов по компонентам (`componentsResult`) и цветам (`colorsResult`, `colorsResultStroke`).
    *   В UI отправляется сообщение `progress-update` для отображения индикатора загрузки.

2.  **Обработка узлов (Цикл в `figma.ui.onmessage`)**:
    *   Плагин итерирует по собранному списку узлов (`nodesToProcess`).
    *   Для каждого узла вызывается внутренняя логика обработки (в текущей реализации это происходит непосредственно в цикле `check-all`, вызывая `processNodeColors` и `processNodeComponent` при необходимости).
    *   Периодически (каждые 5 узлов) отправляется сообщение `progress-update` в UI для обновления прогресс-бара. Добавляется небольшая задержка (`delay`) для предотвращения блокировки UI.

3.  **Анализ компонентов (`processNodeComponent`)**:
    *   Если текущий узел является `INSTANCE` или `COMPONENT`:
        *   Асинхронно получается главный компонент (`node.getMainComponentAsync`).
        *   Определяется, является ли инстанс вложенным в другой инстанс (`isNestedInstance`). Вложенные инстансы помечаются как `skipUpdate`.
        *   Извлекается описание и версия компонента (`getDescription`).
        *   Вызывается `checkComponentUpdate` для асинхронной проверки актуальности компонента из библиотеки. Результат проверки (устарел ли, версия из библиотеки) добавляется к данным компонента.
        *   Определяется, является ли компонент иконкой, на основе его размеров и имени.
        *   Извлекаются пользовательские данные (`pluginDataKey`, `pluginDataVersion`) из `pluginData` узла или его главного компонента.
        *   Собираются все релевантные данные об инстансе (имя, ID, ключ, размеры, статус скрытия, родительский компонент, статус актуальности, версия и т.д.) в объект `componentData`.
        *   Объект `componentData` добавляется в массив `componentsResult.instances`. Обновляются счетчики компонентов и иконок.

4.  **Анализ цветов (`processNodeColors`)**:
    *   Если текущий узел имеет заливки или обводки (`hasFillOrStroke`):
        *   Итерируется по заливкам (`node.fills`) и обводкам (`node.strokes`).
        *   Для каждого видимого SOLID цвета:
            *   Цвет конвертируется в HEX формат (`rgbToHex`).
            *   Если у узла есть привязанные переменные (`node.boundVariables`), вызывается `processVariableBindings` для получения имени переменной и коллекции.
            *   Применяются фильтры для исключения служебных цветов (например, черного/белого в группах "source").
            *   Информация о цвете (HEX, имя переменной, коллекция) добавляется в объект `nodeData`.
        *   Если узел имеет обработанные заливки, `nodeData` добавляется в `colorsResult.instances`.
        *   Если узел имеет обработанные обводки, `nodeData` добавляется в `colorsResultStroke.instances`.

5.  **Завершение анализа и отправка результатов (`figma.ui.onmessage` -> `check-all`)**:
    *   После обработки всех узлов, результаты компонентов (`componentsResult.instances`) сортируются по имени главного компонента (с учетом специальных символов в начале).
    *   Данные о цветах сохраняются в глобальной переменной `lastColorsData`.
    *   В UI отправляется сообщение `all-results`, содержащее полные результаты анализа компонентов, цветов (заливок и обводок), а также структуру дерева выбранных элементов для отладки.

6.  **Отображение результатов в UI (`window.onmessage` в `ui.html`)**:
    *   UI получает сообщение `all-results`.
    *   Скрывается индикатор загрузки.
    *   Обновляются заголовки вкладок с общим количеством найденных элементов.
    *   Результаты компонентов и иконок группируются по ключу главного компонента (`mainComponentKey`).
    *   Результаты цветов (заливок и обводок) фильтруются (исключаются цвета из определенных библиотек) и группируются по HEX коду или имени переменной.
    *   Вызывается `displayGroups` для каждого типа сгруппированных результатов (`instances`, `icons`, `colors`, `colorsStroke`), которая рендерит списки в соответствующих вкладках.
    *   Необработанные данные анализа отображаются в древовидной структуре в Debug вкладке (`createDebugTree`).

7.  **Взаимодействие с элементами в UI**:
    *   При клике на иконку или имя элемента в списках результатов, вызывается обработчик, который отправляет сообщение `scroll-to-node` в backend с ID выбранного узла.
    *   При клике на ссылку "Select all" в заголовке группы, отправляется сообщение `select-nodes` в backend с массивом ID всех узлов в этой группе.
    *   При наведении на иконку элемента, вызывается `showPopover` для отображения детальной информации о нем.

8.  **Обработка метаданных компонентов (`get-component-data`, `set-component-data`, `clear-component-data`)**:
    *   При клике на соответствующие кнопки в UI, отправляются сообщения `get-component-data`, `set-component-data`, или `clear-component-data` в backend.
    *   Backend (`figma.ui.onmessage`) обрабатывает эти сообщения, используя `node.getPluginData` и `node.setPluginData` на выбранных узлах (`COMPONENT` или `COMPONENT_SET`).
    *   Backend отправляет сообщение `component-data-result`, `component-data-set`, или `component-data-cleared` обратно в UI с результатом операции.
    *   UI (`window.addEventListener('message', ...)` второй обработчик) получает эти сообщения и отображает результат в области `#componentDataOutput` с помощью `displayComponentData` или `displayResult`.

## ⚙️ Настройки и конфигурация

### Permissions в `manifest.json`:
-   `teamlibrary`: Требуется для импорта компонентов из библиотек команды (`figma.importComponentByKeyAsync`, `figma.importComponentSetByKeyAsync`).
-   `enablePrivatePluginApi`: Требуется для доступа к некоторым API, таким как работа с переменными (`figma.variables.*`).
-   `documentAccess: "dynamic-page"`: Позволяет плагину получать доступ к узлам на любой странице документа, а не только на текущей.
-   `networkAccess`: Требуется для загрузки изображения на Splash Screen (с `api.vladey.net`).

### Технические аспекты и оптимизации:
-   **Кэширование**: `componentUpdateCache` используется для хранения результатов проверки актуальности компонентов, чтобы избежать повторного импорта одних и тех же компонентов из библиотеки.
-   **Асинхронные операции**: Использование `async/await` и `Promise.all` для асинхронных вызовов Figma API (например, `getNodeByIdAsync`, `importComponentByKeyAsync`, `getVariableByIdAsync`) предотвращает блокировку основного потока плагина.
-   **Пакетная обработка и задержки**: В цикле обработки узлов (`check-all`) прогресс обновляется пакетно (каждые 5 узлов) с небольшой задержкой (`delay`) для улучшения отзывчивости UI.
-   **Фильтрация**: Исключаются скрытые узлы (`isNodeOrParentHidden`) и определенные служебные цвета для уменьшения шума в результатах. Вложенные инстансы помечаются как `skipUpdate` для пропуска проверки актуальности.
-   **Обработка ошибок**: Глобальные обработчики ошибок и `try...catch` блоки используются для перехвата и логирования ошибок, а также для graceful degradation или перезапуска плагина при критических сбоях.

## 🚀 Использование

1.  **Выберите** фрейм, компонент, группу или отдельные элементы в Figma, которые вы хотите проанализировать.
2.  **Запустите** плагин ConsistenSee.
3.  Нажмите кнопку **"Всегда готов!"** на Splash Screen или **"New search"** на главном экране.
4.  **Просмотрите** результаты анализа во вкладках "All instances", "Colors", "Icons".
5.  **Кликните** на иконку или имя элемента в списке, чтобы плагин прокрутил вид к этому элементу в документе и выделил его.
6.  В заголовках групп с несколькими элементами используйте иконку "Select all" для одновременного выделения всех элементов этой группы.
7.  Используйте вкладку "Metadata" для чтения, установки или очистки пользовательских данных (`pluginData`) на выбранных компонентах или наборах компонентов.
8.  Используйте вкладку "Debug" для просмотра необработанных данных анализа и поиска по ним.

Плагин особенно полезен для:
-   Аудита дизайн-системы и выявления отклонений.
-   Поиска и замены устаревших компонентов.
-   Анализа использования цветовых стилей и переменных.
-   Поддержания консистентности в больших и сложных проектах.
-   Миграции на новые версии компонентов или токенов.

## 📚 Описание функций

Ниже приведено детальное описание ключевых функций плагина:

### Backend (`code.js`) Функции

#### `getComponentCacheKey(component)`
-   **Назначение**: Генерирует уникальный ключ для кэширования результатов проверки актуальности компонента.
-   **Детали**: Если компонент является частью `COMPONENT_SET`, ключ формируется как `${ключ_набора}_${имя_компонента}`. Для одиночных компонентов используется их собственный `key`. Включает базовую обработку случаев с отсутствующими ключами.
-   **Вызывается из**: `checkComponentUpdate`.

#### `checkComponentUpdate(mainComponent)`
-   **Назначение**: Асинхронно проверяет, устарел ли главный компонент (`mainComponent`) по сравнению с его версией в библиотеке Figma.
-   **Детали**: Использует `figma.importComponentByKeyAsync` или `figma.importComponentSetByKeyAsync` для импорта актуальной версии из библиотеки по ключу компонента/набора. Сравнивает ID импортированного компонента с ID `mainComponent`. Извлекает версию и описание из импортированного компонента с помощью `getDescription`. Возвращает объект с информацией о статусе (`isOutdated`), ID и версии компонента из библиотеки.
-   **Вызывает**: `getComponentCacheKey`, `figma.importComponentByKeyAsync`, `figma.importComponentSetByKeyAsync`, `getDescription`.
-   **Вызывается из**: `processNodeComponent`.

#### `getNodePath(node)`
-   **Назначение**: Формирует строку, представляющую путь узла в иерархии документа, состоящую из ID родительских узлов, разделенных запятыми.
-   **Детали**: Рекурсивно поднимается по родителям узла, собирая их ID.
-   **Вызывается из**: (В текущей версии кода не используется).

#### `getDescription(node, mainComponent = null)`
-   **Назначение**: Извлекает описание узла и пытается найти в нем строку версии в формате `v X.Y.Z`.
-   **Детали**: Приоритет отдается описанию `mainComponent`, если он передан. Иначе используется описание самого узла. Для инстансов без прямого описания пытается получить описание главного компонента. Парсит версию с помощью регулярного выражения. Возвращает объект `{ description, nodeVersion }`.
-   **Вызывает**: `node.getMainComponentAsync` (для инстансов, если `mainComponent` не передан).
-   **Вызывается из**: `checkComponentUpdate`, `processNodeComponent`, `processComponentSetNode`.

#### `isNodeOrParentHidden(node)`
-   **Назначение**: Проверяет, скрыт ли данный узел или любой из его родительских узлов.
-   **Детали**: Поднимается по иерархии родителей, проверяя свойство `visible`.
-   **Вызывается из**: `processNodeColors`, `processNodeComponent`, `processComponentSetNode`.

#### `isNestedInstance(node)`
-   **Назначение**: Определяет, является ли узел инстансом, вложенным в другой инстанс.
-   **Детали**: Поднимается по иерархии родителей, ища родителя типа `INSTANCE`.
-   **Вызывается из**: `processNodeComponent`.

#### `delay(ms)`
-   **Назначение**: Создает асинхронную задержку выполнения.
-   **Детали**: Возвращает Promise, который разрешается через указанное количество миллисекунд.
-   **Вызывается из**: `updateProgress`.

#### `updateProgress(phase, processed, total, message)`
-   **Назначение**: Отправляет сообщение в UI для обновления индикатора прогресса.
-   **Детали**: Постит `pluginMessage` типа `progress-update` с текущим этапом, количеством обработанных/всего элементов и сообщением. Включает небольшую задержку для синхронизации с UI.
-   **Вызывает**: `figma.ui.postMessage`, `delay`.
-   **Вызывается из**: `figma.ui.onmessage` (в цикле обработки `check-all`).

#### `figma.ui.onmessage = async (msg) => { ... }`
-   **Назначение**: Центральный обработчик сообщений, получаемых backend от UI.
-   **Детали**: Обрабатывает сообщения разных типов (`resize`, `check-all`, `scroll-to-node`, `select-nodes`, `get-component-data`, `set-component-data`, `clear-component-data`). Команда `check-all` инициирует основной процесс анализа. Команды навигации и работы с данными компонентов взаимодействуют с Figma API для выполнения действий в документе.
-   **Вызывает**: `figma.currentPage.selection`, `node.findAll`, `processNodeColors`, `processNodeComponent`, `figma.ui.postMessage`, `figma.getNodeByIdAsync`, `figma.currentPage = page`, `figma.viewport.scrollAndZoomIntoView`, `figma.currentPage.selection = [...]`, `Promise.all`, `figma.notify`, `figma.closePlugin`, `node.getPluginData`, `node.setPluginData`.
-   **Обрабатывает сообщения**: `resize`, `check-all`, `scroll-to-node`, `select-nodes`, `get-component-data`, `set-component-data`, `clear-component-data`.

#### `hasFillOrStroke(node)`
-   **Назначение**: Проверяет, имеет ли узел видимые сплошные заливки или обводки.
-   **Детали**: Проверяет массивы `node.fills` и `node.strokes` на наличие элементов с `type === 'SOLID'` и `visible !== false`.
-   **Вызывается из**: `figma.ui.onmessage` (в цикле обработки `check-all`).

#### `rgbToHex({ r, g, b })`
-   **Назначение**: Конвертирует цвет из формата RGB (компоненты 0-1) в HEX строку (`#RRGGBB`).
-   **Детали**: Масштабирует компоненты в диапазон 0-255, округляет, конвертирует в 16-ричный формат и форматирует строку. Включает обработку ошибок.
-   **Вызывается из**: `processNodeColors`.

#### `processNodeColors(node, colorsResult, colorsResultStroke)`
-   **Назначение**: Анализирует цветовые свойства узла (заливки и обводки), извлекает информацию о цвете и привязанных переменных.
-   **Детали**: Итерирует по заливкам и обводкам. Для видимых SOLID цветов конвертирует цвет в HEX (`rgbToHex`). Если есть привязанные переменные, вызывает `processVariableBindings`. Применяет фильтры для исключения определенных цветов/групп. Добавляет собранные данные в `colorsResult.instances` или `colorsResultStroke.instances`.
-   **Вызывает**: `isNodeOrParentHidden`, `rgbToHex`, `processVariableBindings`, `node.parent`.
-   **Вызывается из**: `figma.ui.onmessage` (в цикле обработки `check-all`).

#### `processNodeComponent(node, componentsResult)`
-   **Назначение**: Анализирует узел типа INSTANCE или COMPONENT, собирает информацию о нем и его главном компоненте, проверяет актуальность.
-   **Детали**: Получает главный компонент (`getMainComponentAsync`). Определяет, вложен ли инстанс (`isNestedInstance`). Вызывает `checkComponentUpdate` для проверки актуальности. Определяет, является ли иконкой. Извлекает `pluginData`. Собирает все данные в объект и добавляет его в `componentsResult.instances`, обновляя счетчики.
-   **Вызывает**: `node.getMainComponentAsync`, `getDescription`, `checkComponentUpdate`, `isNodeOrParentHidden`, `node.getPluginData`, `node.parent`.
-   **Вызывается из**: `figma.ui.onmessage` (в цикле обработки `check-all`).

#### `processComponentSetNode(node, parentSet = null)`
-   **Назначение**: Обрабатывает узел типа COMPONENT_SET.
-   **Детали**: Извлекает описание и версию набора (`getDescription`). Формирует объект данных для самого набора. (Примечание: в основном цикле анализа `check-all` дочерние компоненты набора обрабатываются отдельно функцией `processNodeComponent`).
-   **Вызывает**: `getDescription`, `isNodeOrParentHidden`.
-   **Вызывается из**: (В основном цикле анализа `check-all` не используется для рекурсивного обхода, дети обрабатываются напрямую).

#### `checkComponentUpdates(componentsResult)`
-   **Назначение**: Итерирует по списку компонентов и проверяет актуальность каждого.
-   **Детали**: Проходит по `componentsResult.instances`, находит главный компонент, вызывает `checkComponentUpdate`, обновляет данные инстанса и отправляет частичные результаты в UI. Пропускает вложенные инстансы.
-   **Вызывает**: `figma.getNodeById`, `checkComponentUpdate`, `figma.ui.postMessage`.
-   **Вызывается из**: (В текущей версии кода не вызывается из основного цикла `check-all`. Проверка актуальности интегрирована в `processNodeComponent`).

#### `processVariableBindings(node, nodeData, propertyType, prefix)`
-   **Назначение**: Асинхронно извлекает информацию о переменных, привязанных к заливкам или обводкам узла.
-   **Детали**: Проверяет `node.boundVariables` для указанного типа свойства (`fills` или `strokes`). Использует `figma.variables.getVariableByIdAsync` и `figma.variables.getVariableCollectionByIdAsync` для получения данных о переменной и ее коллекции. Добавляет имя переменной, имя и ID коллекции в `nodeData`.
-   **Вызывает**: `figma.variables.getVariableByIdAsync`, `figma.variables.getVariableCollectionByIdAsync`.
-   **Вызывается из**: `processNodeColors`.

### Frontend (`ui.html`) Функции

#### `displayResult(message, isError = false)`
-   **Назначение**: Отображает простое текстовое сообщение в области вывода метаданных (`#componentDataOutput`).
-   **Детали**: Очищает область, создает параграф с сообщением, стилизует его в зависимости от `isError`.
-   **Вызывается из**: Обработчик `window.addEventListener('message', ...)` (второй) для сообщений `component-data-set`, `component-data-cleared`, и ошибок в `component-data-result`.

#### `showPopover(icon, instance)`
-   **Назначение**: Отображает всплывающее окно (popover) с детальной информацией об инстансе при наведении на его иконку в списке результатов.
-   **Детали**: Создает элемент `div.popover`, форматирует все свойства объекта `instance` в HTML, позиционирует popover рядом с иконкой и делает его видимым. Удаляет popover при уходе курсора.
-   **Вызывается из**: `displayGroups` (в обработчике `mouseenter` для иконок элементов).

#### `displayGroups(groupedData, targetList)`
-   **Назначение**: Рендерит сгруппированные результаты анализа (компоненты или цвета) в указанный список в UI.
-   **Детали**: Очищает целевой список (`#resultsList`, `#iconResultsList`, `#colorResultsList`, `#colorStrokeResultsList`). Итерирует по группам данных. Для групп из одного элемента (кроме вкладок цветов) отображает его напрямую. Для групп из нескольких элементов создает сворачиваемый заголовок группы с именем, счетчиком и ссылкой "Select all". Внутри сворачиваемой части отображает каждый элемент группы. Добавляет иконки, родительские имена, метки скрытия, версии/описания, цветовые образцы/информацию. Добавляет обработчики кликов для сворачивания/разворачивания групп и навигации/выделения элементов.
-   **Вызывает**: `createIcon`, `showPopover`, `parent.postMessage` (`scroll-to-node`, `select-nodes`).
-   **Вызывается из**: `window.onmessage` (для сообщения `all-results`).

#### `createIcon(type)`
-   **Назначение**: Создает элемент SVG `<svg>` с элементом `<use>`, ссылающимся на соответствующую иконку из SVG спрайта, определенного в `ui.html`.
-   **Детали**: Выбирает символ иконки (`#instance-icon`, `#text-icon`, `#frame-icon`, `#rectangle-icon`, `#info-icon`) на основе типа узла Figma. Добавляет CSS классы для стилизации.
-   **Вызывается из**: `displayGroups`.

#### `createDebugTree(data, searchTerm = '')`
-   **Назначение**: Рекурсивно строит HTML-представление объекта или массива в виде сворачиваемого дерева для отображения в Debug вкладке. Поддерживает поиск и подсветку совпадений.
-   **Детали**: Использует теги `<details>` и `<summary>` для создания сворачиваемых секций. Рекурсивно вызывает себя для вложенных объектов и массивов. Для примитивных значений вызывает `createValueElement`. Добавляет отступы и стилизует ключи и значения. Открывает ветки дерева, содержащие совпадения с поисковым запросом.
-   **Вызывает**: `createDebugTree` (рекурсивно), `createValueElement`.
-   **Вызывается из**: `window.onmessage` (для сообщения `all-results`).

#### `createValueElement(value, searchTerm = '')`
-   **Назначение**: Создает элемент `<span>` для отображения примитивного значения (строка, число, булево) в дереве отладки.
-   **Детали**: Устанавливает текстовое содержимое и применяет CSS классы (`string`, `number`, `boolean`) для синтаксической подсветки. Если значение совпадает с поисковым запросом, добавляет класс `highlight`.
-   **Вызывается из**: `createDebugTree`.

#### `window.onmessage = async (event) => { ... }` (Первый обработчик)
-   **Назначение**: Основной обработчик сообщений, получаемых UI от backend плагина.
-   **Детали**: Обрабатывает сообщения типов `user-info`, `progress-update`, `all-results`, `error`. Обновляет UI: отображает информацию о пользователе, обновляет прогресс-бар, рендерит результаты анализа компонентов и цветов, отображает ошибки.
-   **Вызывает**: DOM-манипуляции для обновления текста и стилей, `displayGroups`, `createDebugTree`, `displayResult` (для ошибок).
-   **Обрабатывает сообщения**: `user-info`, `progress-update`, `all-results`, `error`.

#### `window.addEventListener('message', function(event) { ... })` (Второй обработчик)
-   **Назначение**: Дополнительный обработчик сообщений, специфичный для результатов операций с метаданными компонентов.
-   **Детали**: Обрабатывает сообщения типов `component-data-result`, `component-data-set`, `component-data-cleared`. Вызывает `displayComponentData` или `displayResult` для отображения результатов в области `#componentDataOutput`.
-   **Вызывает**: `displayComponentData`, `displayResult`.
-   **Обрабатывает сообщения**: `component-data-result`, `component-data-set`, `component-data-cleared`.

## 🔗 Дополнительные ресурсы

Для более глубокого понимания работы плагина и доступных возможностей Figma API, обратитесь к официальной документации:

-   [Figma Plugin API Documentation](https://www.figma.com/plugin-docs/)
